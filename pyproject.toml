[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meshbridge"
version = "0.1.0"
description = "Discord to MeshCore bridge"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["meshbridge*"]
